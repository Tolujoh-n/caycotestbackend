JWT_SECRET=
JWT_EXPIRE=7d
NODE_ENV=
MONGODB_URI=
FRONTEND_URL=

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
# Set to true to skip sending (log links to console) when SMTP is blocked e.g. locally
# EMAIL_DEV_SKIP_SEND=false

